<%- include('../partials/head', { title: 'Adidas Products' }) %>

<div class="container" role="main" aria-label="All Products">
  <h1 class="page-title">All Products</h1>
  
  <section class="product-grid" aria-live="polite">
    <% products.forEach(product => { %>
      <article class="product-card" aria-labelledby="product-<%= product._id %>-name">